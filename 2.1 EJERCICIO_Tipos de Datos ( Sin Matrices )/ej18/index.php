
    <?php
    // Declarar una variable entera
    $numero = 10;

    // Usar la variable en una operación (por ejemplo, multiplicar por 2)
    $resultado = $numero * 2;

    // Imprimir el resultado en pantalla
    echo "El resultado de multiplicar " . $numero . " por 2 es: " . $resultado;
    ?>
